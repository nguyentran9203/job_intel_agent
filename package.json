{
  "name": "eu-job-intel-agent",
  "version": "1.0.0",
  "description": "EU frontend job market intelligence agent",
  "type": "module",
  "scripts": {
    "build":    "cd dashboard && npm install && npm run build",
    "start":    "node src/server.js",
    "pipeline": "node src/pipeline.js",
    "dev:api":  "node src/server.js",
    "dev:ui":   "cd dashboard && npm run dev"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.36.3",
    "better-sqlite3": "^11.3.0",
    "dotenv": "^16.4.5",
    "node-fetch": "^3.3.2",
    "nodemailer": "^6.9.14",
    "p-limit": "^6.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
